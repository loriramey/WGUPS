import importlib
import pickle

import packaging

from setuptools import Distribution


def test_reimport_extern():

    """
    Test reimport extern function.

    Args:
        (describe parameters here)

    Returns:
        (describe return value here)
    """
        packaging2 = importlib.import_module(packaging.__name__)
    assert packaging is packaging2


def test_distribution_picklable():

    """
    Test distribution picklable function.

    Args:
        (describe parameters here)

    Returns:
        (describe return value here)
    """
        pickle.loads(pickle.dumps(Distribution()))
